{
  "name": "my-tech",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pagefind": "pagefind --site dist",
    "build:search": "astro build && pagefind --site dist"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.14",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
