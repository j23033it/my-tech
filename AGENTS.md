# AGENTS.md

## プロジェクト方針

このリポジトリは、個人用の「テック系のあらゆる技術を自分向けに解説するサイト」として運用する。

当初はウェブ技術の入門知識を1ページで解説する静的HTMLサイトだったが、今後はより広い定義の技術解説サイトへ拡張する。単体HTMLにすべてを詰め込む構成ではなく、カテゴリ別インデックスと記事ページによる画面遷移を前提にする。

## 推奨技術スタック

- フレームワーク: Astro
- 記事形式: Markdown / MDX
- 記事管理: Astro Content Collections
- 検索: Pagefind
- 図解: Mermaid
- UI: Astro Components を基本とし、必要な箇所だけ Vanilla JavaScript を使う
- スタイリング: 既存HTML/CSSの雰囲気を継承し、まずは素のCSSまたはAstro内のCSSで管理する
- リポジトリ管理: GitHub
- デプロイ先: Cloudflare Pages

## 技術選定の理由

Astroは、Markdown/MDX中心の静的コンテンツサイトに向いている。現在の単体HTMLサイトが持つ軽さを保ちながら、記事分割、カテゴリ、タグ、目次、検索、SEO用メタ情報を管理しやすくできる。

Next.jsは将来的なWebアプリ化、会員機能、DB連携などには強いが、このプロジェクトの主目的は解説記事の蓄積であるため、現時点では過剰と判断する。

VitePressやDocusaurusも候補になり得るが、個人用の技術解説メディアとしてデザインや情報設計を柔軟に作りたい場合はAstroを第一候補とする。

## デプロイ方針

デプロイ先はCloudflare Pagesを基本とする。

理由:

- 静的サイト配信と相性がよい
- Astro公式のデプロイ手順がある
- 個人用サイトとして十分な無料枠がある
- GitHub連携による自動デプロイがしやすい
- CDN配信、HTTPS、カスタムドメイン運用に向いている

VercelやNetlifyも代替候補として利用可能だが、このプロジェクトではCloudflare Pagesを標準のデプロイ先として扱う。

## 想定ディレクトリ構成

将来的には以下のような構成を目指す。

```text
src/
  content/
    articles/
      web/
      frontend/
      backend/
      database/
      infra/
      ai/
      security/
      tools/
  layouts/
  pages/
  components/
  styles/
```

## 想定カテゴリ

- Web基礎
- フロントエンド
- バックエンド
- データベース
- インフラ
- AI
- セキュリティ
- 開発ツール
- アーキテクチャ
- コンピュータサイエンス基礎

## 記事作成ルール

記事はMarkdownまたはMDXで作成する。記事ごとにfrontmatterを持たせ、少なくとも以下の情報を管理する。

```yaml
title: ""
description: ""
category: ""
tags: []
level: "beginner"
updated: "YYYY-MM-DD"
```

本文は、自分があとから読み返して理解できることを優先する。一般公開向けのマーケティング文ではなく、概念、背景、具体例、使いどころ、関連技術との違いを整理する。

## 画面遷移と情報設計

トップページは総合インデックスとして扱う。

カテゴリページでは、そのカテゴリに属する記事一覧を表示する。記事ページでは本文、目次、関連カテゴリ、関連タグ、前後記事への導線を用意する。

検索はPagefindを使い、静的サイトのまま全文検索できる構成を目指す。

## 既存HTMLの扱い

既存の `index.html` と `web_technology_complete_guide.html` は、現在のサイト内容を保持する資料として扱う。

Astro移行時は、既存HTMLを一度に捨てず、以下の順序で段階的に移行する。

1. Astroプロジェクトを作成する
2. 既存HTMLのデザイン方針をAstroのレイアウトとCSSへ移す
3. 既存本文をカテゴリ別の記事に分割する
4. トップページを総合インデックスに作り替える
5. Pagefindによる検索を追加する
6. Cloudflare Pagesへデプロイする

## 作業上の注意

- 作業結果の説明、ネクストステップ、ソースコード内コメントは日本語で書く
- `rg` は使わない
- 不明点がある場合は、曖昧なまま進めずユーザーに確認する
- 既存の変更を勝手に戻さない
- 技術選定は、過剰な機能よりも長期的に書き続けやすいことを優先する
